<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Menu">
	<insert id="insert" parameterType="Menu">
		insert into menu(menu_name, menu_price, menu_desc, menu_image, shop_idx)
		values(#{menuName}, #{menuPrice}, #{menuDesc}, #{menuImage}, #{shop.shopIdx})
	</insert>
	
	<select id="select" parameterType="int" resultType="Menu">
		select * from menu where menu_idx=#{menuIdx} order by menu_name asc
	</select>
	
	<select id="selectByShop" parameterType="int" resultType="Menu">
		select * from menu where shop_idx=#{shopIdx} order by menu_name asc
	</select>
	
	<delete id="delete">
		delete from menu where menu_idx=#{menuIdx}
	</delete>
</mapper>